"use strict";
//exercise 12
let members = ['sana', 'rida', 'sidra', 'mishawal', 'areeba'];
let mesage = 'kya hal hy:';
for (let i = 0; i < members.length; i++) {
    console.log(mesage + members[i]);
}
